Deep learning models for prediction of futute solar power generation. 
The models generats solar intensity based on different weather conditions used as input features.
Different models are used to predict solar energy like Long-short-term-memory(LSTM), Bidirectional LSTM, Gated Recurrent Unit.
